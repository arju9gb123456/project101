import React from 'react';
import ReactDOM from 'react-dom/client';
import DreamCareerApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DreamCareerApp />
  </React.StrictMode>
);